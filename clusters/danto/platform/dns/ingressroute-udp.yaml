apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: hickory-dns-udp
spec:
  ingressClassName: traefik
  entryPoints:
    - dns-udp
  routes:
    - services:
        - name: hickory-dns
          port: dns-udp
