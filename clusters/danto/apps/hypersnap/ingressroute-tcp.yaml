apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: hypersnap-grpc
spec:
  ingressClassName: traefik
  entryPoints:
    - hypersnap-grpc
  routes:
    - match: HostSNI(`*`)
      services:
        - name: hypersnap
          port: grpc
